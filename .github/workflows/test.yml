name: Test the Action

on:
  workflow_dispatch: 

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: ./action.yml
        with:
          astro_key_id: asdf
          astro_key_secret: asdf
          github_secret: ${{ secrets.GITHUB_TOKEN }}