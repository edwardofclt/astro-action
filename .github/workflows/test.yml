name: Test the Action

on:
  workflow_dispatch: 

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: edwardofclt/astro-action@2a808d5277287af075f9cf4a0182712221010cb6
        with:
          astro_key_id: asdf
          astro_key_secret: asdf