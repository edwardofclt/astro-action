name: Test the Action

on:
  workflow_dispatch: 

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: edwardofclt/astro-action@6d62e86852a6eea3176191126300a3f97f55ae8b
        with:
          astro_key_id: asdf
          astro_key_secret: asdf